package schedule

// Run `go generate ./...` to expand the .warchest files in this package into
// their sibling .go files (schedule.warchest -> schedule.go). go generate
// only scans .go files for directives, so this lives here.
//
//go:generate go tool warchest generate
